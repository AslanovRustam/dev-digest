/** Constants for HoverCard. */

/** Hover/focus intent before the card opens (skips drive-by mouse passes). */
export const OPEN_DELAY_MS = 150;

/** Grace period to move the pointer from the trigger into the card. */
export const CLOSE_DELAY_MS = 120;

/** Gap between the trigger and the card, px. */
export const TRIGGER_GAP = 6;

/** Minimum distance kept from the viewport edges, px. */
export const VIEWPORT_MARGIN = 8;

export const DEFAULT_WIDTH = 380;
